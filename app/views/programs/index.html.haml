%p#notice= notice
%h2{:style => "font-weight: bold;"} Programs:
%br
  - if admin_signed_in?
    = link_to 'Add Program', new_program_path,class: 'btn btn-success'
  - if user_signed_in?
    = link_to 'Back',page_instruction_path,class: 'btn btn-success'

  %table.table.table-hover
    %thead

      %tr
        %th Language
        %th Description
        %th{:colspan => "3"}
    %tbody
      - @programs.each do |program|
        %tr
          %td{:style => "font-weight: bold;"}= link_to program.language,program_levels_path(program)
          %td= program.description
          - if admin_signed_in?
            %td= link_to 'Edit', edit_program_path(program),class: 'btn btn-warning'
            %td= link_to 'Delete', program, method: :delete, data: { confirm: 'Are you sure?' },class: 'btn btn-danger'
  %br